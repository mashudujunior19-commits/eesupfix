import 'package:flutter/material.dart';

class OrdersPoolTab extends StatelessWidget {
  const OrdersPoolTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}